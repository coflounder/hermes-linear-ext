# Danka managed-service config.
# The GitHub App receives check events and runs the server-side merge-decision pipeline
# after every required check below has completed successfully.
version: 1

required_checks:
  - danka-ready
  - pure-tests

# Start new repos in decision/review mode. Raise once calibration proves safe.
trust_level: "0"
